# HW5: Ontology-based Semantic Grounding — Group 02

> AI Capstone 2026, Spring 2026

## Group Members

| Member | Role |
|--------|------|
| (Person A) | Repo setup, ontology skeleton |
| (Person B) | GraspableObject inference rule |
| (Person C) | Cup stacking instances |
| (Person D) | Baseline object instances |
| (Person E) | OWL reasoning, SPARQL queries |
| (Person F) | Report, documentation |

<!-- TODO (Person F): Fill in actual member names -->

---

## Selected Task

Primary task: **Cup Stacking** (blue cup and pink cup)

Baseline tasks also included as required: Cutlery Arrangement, Toy Block Collection.

---

## Repository Structure

```
semantic-affordance-grounding/
├── README.md
├── report.md
├── ontology/
│   ├── group-ontology.ttl       # Group 02 authored ontology
│   ├── inferred-results.ttl     # Inferred graph after OWL reasoning
│   └── imports/
│       ├── course-affordance.ttl
│       └── course-alignment.ttl
├── queries/
│   ├── graspable_objects.rq
│   └── task_objects.rq
├── results/
│   ├── graspable_objects_output.txt
│   └── screenshots/
└── src/
```

---

## Namespace Policy

| Prefix | URI | Usage |
|--------|-----|-------|
| `cap:` | `https://hcis.io/ontology/aicapstone/2026/` | Shared course vocabulary |
| `g02:` | `https://hcis.io/ontology/aicapstone/2026/group02/` | Group 02 instances and extensions |

Group-specific individuals (e.g., `g02:blueCup01`) use the `g02:` namespace.
Shared course classes and properties (e.g., `cap:Cup`, `cap:hasAffordance`) use the `cap:` namespace.

---

## Ontology Design

<!-- TODO (Person F): Fill in after ontology is complete -->

---

## Modeled Objects and Affordances

| Instance | Type | Affordance | Task Role |
|----------|------|-----------|-----------|
| `g02:blueCup01` | `cap:Cup` | `cap:GraspingAffordance` | `cap:TargetObject` |
| `g02:pinkCup01` | `cap:Cup` | `cap:GraspingAffordance` | `cap:TargetObject` |
| `g02:knife01` | `cap:Knife` | `cap:GraspingAffordance` | `cap:TargetObject` |
| `g02:fork01` | `cap:Fork` | `cap:GraspingAffordance` | `cap:TargetObject` |
| `g02:plate01` | `cap:Plate` | `cap:SupportAffordance` | `cap:ReferenceObject` |
| `g02:toyBlock01` | `cap:ToyBlock` | `cap:GraspingAffordance` | `cap:CollectableObject` |
| `g02:basket01` | `cap:Basket` | `cap:ContainmentAffordance` | `cap:ContainerTarget` |

---

## Reasoning Workflow

<!-- TODO (Person F): Fill in after Person E completes reasoning -->

Toolchain: Protégé + HermiT reasoner

`ontology/inferred-results.ttl` was generated by ...

---

## Running the SPARQL Query

```bash
# Option A: Apache Jena (arq)
arq --data ontology/inferred-results.ttl --query queries/graspable_objects.rq

# Option B: Protégé SPARQL tab
# Load inferred-results.ttl, open SPARQL query tab, paste query content
```

## Expected Query Output

<!-- TODO (Person F): Fill in after Person E runs the query -->

---

## What is Inferred vs. Asserted

- **Asserted**: each object instance's type, affordance, task role, color, label
- **Inferred**: `cap:GraspableObject` membership — derived by the reasoner from the `owl:equivalentClass` axiom, not manually written

---

## Links

- [Group Ontology](ontology/group-ontology.ttl)
- [Inferred Results](ontology/inferred-results.ttl)
- [SPARQL Query](queries/graspable_objects.rq)
- [Query Output](results/graspable_objects_output.txt)
- [Course Ontology](ontology/imports/course-affordance.ttl)
